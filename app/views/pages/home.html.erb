<div class="container-sm">
  <div class="cards">
    <% @groups.each do |group| %>
      <div class="card m-1">
        <img src="https://source.unsplash.com/collection/2706932/" />
        <div class="card-infos">
          <div>
            <h5 class="card-index-info"><a href=""></a><%= link_to group.name, group_path(group), :id => "index-info"%></h5>
            <p> <%= group.event_type %> - <%=group.size %> friends </p>
          </div>
          <h2 class="card-pricing">€ <%= group.hourly_rate.round(2) %> /h</h2>
          <img src="https://source.unsplash.com/collection/9583767/" class="card-user avatar-bordered" />
        </div>
      </div>
    <% end %>
  </div>
</div>
